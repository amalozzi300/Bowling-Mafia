Terminal Commands:

// Enter bowlers, hdcps, and desired sidepots into "bowlers.txt"
    python3 sidepots.py < bowlers.txt

// Repeat following until it runs (might need to run multiple times to form teams)
    python3 rand.py < mdBowlers.txt > mdTeams.txt

// Repeat following for each game:
// Add scores to "scores.txt"
    ./run.sh    
